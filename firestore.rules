rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow logged-in users full access (DEV ONLY)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
